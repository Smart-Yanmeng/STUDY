server:
  port: 8888

spring:
  application:
    name: JWTDemo

  datasource:
    url: jdbc:mysql://localhost:3306/jwt
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.baizhi.jwtdemo.entity